# run simple test using full patient data
PREPROCESS_BIN='build/dosecalc-preprocess/dosecalc-preprocess'
DOSE_BEAMLET_BIN='build/dosecalc-beamlet/dosecalc-beamlet'
DOSE_BEAM_BIN='build/dosecalc-beam/dosecalc-beam'

#DICOM_DIR='data/ctdata/dosecalc_testdata001_prostate'
DICOM_DIR='data/ctdata/dosecalc_testdata001_prostate'
PHANTOM_VOL='tests/input_files/phantom_water.h5'
PHANTOM_FMAPS='tests/input_files/fmaps_water.h5'
CONFIG='tests/input_files/config.json'

# computation settings
DEV=0
NDEVICES=1

NBEAMS=1 #"$NDEVICES" # test 1beam on each GPU

DEBUG_ARGS="--verbose --timing"

PREPROCESS_FUNC_CALL="$PREPROCESS_BIN --dicom=$DICOM_DIR --config=$CONFIG --nbeams=$NBEAMS --device=$DEV $DEBUG_ARGS"
DOSE_BEAMLET_FUNC_CALL="$DOSE_BEAMLET_BIN --device=$DEV --ndevices=$NDEVICES $DEBUG_ARGS"
DOSE_BEAM_FUNC_CALL="$DOSE_BEAM_BIN --device=$DEV --ndevices=$NDEVICES $DEBUG_ARGS"
